"""enterprise_bi — the Enterprise AI Business Intelligence & Analytics Platform.

A multi-agent, natural-language-to-SQL BI service. Business users ask questions in
plain English; a LangGraph of seven agents (intent → schema-RAG → SQL generation
→ SQL validation → analytics → visualization → insight) returns a summary, a
table, a chart spec, and an optional export — never requiring SQL knowledge.
"""
from __future__ import annotations

__version__ = "0.1.0"
